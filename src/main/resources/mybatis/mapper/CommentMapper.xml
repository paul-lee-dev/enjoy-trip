<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoyTrip.comment.dao.CommentMapper">


    <!-- 댓글 생성 -->
    <insert id="createComment" parameterType="CreateCommentReq">

        insert into comment (user_id, article_id, content)
<!--        <if test="parentCommentId != null">-->
<!--                ,-->
        values (#{userId}, #{subject}, #{content})

    </insert>

    <select id="listComment" parameterType="map" resultType="GetCommentRes">
        select comment_id, user_id, subject, hit, created_at
        from comment
        where
        <include refid="search"></include>
        order by comment_id desc
        limit #{start}, #{listsize}
    </select>

    <!-- 수정 -->
    <update id="modifyComment" parameterType="ModifyCommentReq">
        update comment set subject = #{subject}, content = #{content}
                         where comment_id = #{commentId}
    </update>

    <!-- 조회 수 -->
    <update id="updateHit" parameterType="int">
        update comment
        set hit = hit + 1
        where comment_id = #{commentId}
    </update>

    <!-- 삭제 -->
    <delete id="deleteComment" parameterType="int">
        delete from comment
        where comment_id = #{commentId}
    </delete>
</mapper>