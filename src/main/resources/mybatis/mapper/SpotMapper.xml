<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoyTrip.spot.dao.SpotMapper">

    <!-- getSpot -->
    <select id="getSpotList" parameterType="Map" resultType="getSpotRes">
        select i.attraction_info_id as spotId, title, concat(addr1, ' ', addr2) as address, tel,
               first_image as image, readcount as hit,
               content_type_id as contentId, sido_code as sidoId,
               gugun_code as gugunId, latitude as lat,longitude as lon,
               d.overview as description
        from attraction_info i
             left join attraction_description d on i.attraction_info_id = d.attraction_info_id
        <if test="key != null">
            where
                <if test='key.equals("name") and word != null'>
                    title like concat('%', #{word}, '%') and
                </if>
                <if test='key.equals("type")'>
                    content_type_id = #{contentTypeId} and
                </if>
                <if test='key.equals("region")'>
                    sido_code = #{sidoId} and
                    <if test='gugunId != null'>
                        gugun_code = #{gugunId} and
                    </if>
                </if>
                true
        </if>
            limit ${start}, ${pgsize}
    </select>

    <select id="getSpot" parameterType="int" resultType="getSpotRes">
        select i.attraction_info_id as spotId, title, concat(addr1, ' ', addr2) as address, tel,
                first_image as image, readcount as hit,
                content_type_id as contentId, sido_code as sidoId,
                gugun_code as gugunId, latitude as lat,longitude as lon,
                d.overview as description
        from attraction_info i
                left join attraction_description d on i.attraction_info_id = d.attraction_info_id
        where i.attraction_info_id = #{spotId}
    </select>

    <select id="getSidoList" resultType="string">
        select sido_name
        from sido
    </select>

    <select id="getGugunList" parameterType="int"   resultType="string">
        select gugun_name
        from gugun
        where sido_code = #{sidoId}
    </select>
</mapper>